# Example Environment Variables
# Copy this file to .env.development or .env.production and update values

# API Configuration — FastAPI origin only (NOT the CloudFront/S3 UI URL).
# Production example: http://44.218.45.174:8000
REACT_APP_API_BASE_URL=http://localhost:8000

# Required in production when backend ADMIN_API_TOKEN is set (same value).
# REACT_APP_ADMIN_API_TOKEN=your-admin-token
REACT_APP_ENVIRONMENT=development

# Optional: send admin token for protected API endpoints
# REACT_APP_ADMIN_API_TOKEN=REPLACE_WITH_LONG_RANDOM_SECRET

# Source Maps
# true for development, false for production (security)
GENERATE_SOURCEMAP=true
