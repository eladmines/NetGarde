# Production env template (do not rely on backend/.env.production on EC2 — CI used to overwrite it).
#
# On EC2 the live file is:
#   /etc/trustedge/backend.env
#
# First-time setup:
#   sudo mkdir -p /etc/trustedge
#   sudo cp backend/.env.production.example /etc/trustedge/backend.env
#   sudo chown root:docker /etc/trustedge/backend.env && sudo chmod 640 /etc/trustedge/backend.env
#   sudo nano /etc/trustedge/backend.env
#
# Deploy merges DB_URL / WireGuard keys into /etc/trustedge/backend.env via scripts/ec2-sync-backend-env.sh

DB_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

LOG_TO_FILE=0
LOG_JSON=1
LOG_LEVEL=INFO
LOG_SERVICE=backend
PYTHONUNBUFFERED=1

# WireGuard enroll
VPN_ENDPOINT=44.218.45.174:51820
VPN_SERVER_PUBLIC_KEY=REPLACE_WITH_wg0_PUBLIC_KEY

# Host WireGuard agent (must match systemd service token)
WG_AGENT_URL=http://172.17.0.1:9109
WG_AGENT_TOKEN=REPLACE_WITH_LONG_RANDOM_SECRET

# DNS ingest: false = only blocked queries stored in RDS (default); true = legacy full log
PERSIST_ALL_DNS=false

# Anomaly detection
NEW_DOMAIN_ALERTS=true
BANDWIDTH_ALERT_MIB_PER_SEC=50

DEVICE_TOKEN_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET
DEVICE_TOKEN_TTL_DAYS=365
# ENROLL_BOOTSTRAP_TOKEN=REPLACE_WITH_ENROLL_BOOTSTRAP_SECRET

DNS_INGEST_TOKEN=REPLACE_WITH_LONG_RANDOM_SECRET

ADMIN_API_TOKEN=REPLACE_WITH_LONG_RANDOM_SECRET

# AI network review: template (default) | ollama | openai
# Ollama needs ~6GB free disk; run: sudo bash scripts/ec2-setup-ollama.sh
NETWORK_REVIEW_MODE=template
NETWORK_REVIEW_CACHE_TTL_SEC=90
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2:3b
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
LLM_TIMEOUT_SEC=180

# Parent-facing per-device behavior text: template | ollama | openai (scoring stays rules-based)
BEHAVIOR_REVIEW_MODE=template
BEHAVIOR_REVIEW_CACHE_TTL_SEC=300

# CORS: merged with built-in S3 website + localhost. Add CloudFront if used.
# CORS_ORIGINS=http://localhost:3000,https://daemixzdg8jfd.cloudfront.net
# ALLOWED_ORIGINS=http://trustedge-frontend.s3-website-us-east-1.amazonaws.com
